# acl_core: pure-Python, Python 3.3-compatible core for the AC Leaderboard app.
# Nothing in this package imports the in-game `ac`/`acsys` modules, so it can be
# unit-tested on any machine (e.g. macOS) without Assetto Corsa.
